import { OnInputDirective } from './on-input.directive';

describe('OnInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnInputDirective();
    expect(directive).toBeTruthy();
  });
});
